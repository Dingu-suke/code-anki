<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <meta name="turbo-visit-control" content="reload">
  </head>

<body class="bg-black min-h-screen flex flex-col">
  <div class="drawer xl:drawer-open">
    <input id="my-drawer" type="checkbox" class="drawer-toggle" />
    <div class="drawer-content flex flex-col">
      <!-- 小画面用 ハンバーガーメニュー -->
      <div class="xl:hidden fixed top-0 left-0 m-4">
        <label for="my-drawer" class="btn btn-square btn-ghost">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="inline-block w-6 h-6 stroke-current"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path></svg>
        </label>
      </div>
      <!-- ページ内容 -->
      <div class="p-4">
        <main class="bg-slate-900 rounded-lg shadow-xl p-6 min-h-screen">
          <%= yield %>
        </main>
      </div>
    </div> 
    <div class="drawer-side cursor-default z-50">
      <label for="my-drawer" aria-label="close sidebar" class="drawer-overlay cursor-default"></label>
      <div class="w-[270px] min-h-full bg-gray-900 text-white p-4 cursor-default">
        <%= render 'shared/sidebar' %>
      </div>
    </div>
  </div>
</body>

</html>
